# Etapa 9.6A: plan de refactorización Django para cartones híbridos

Fecha: 2026-06-30.

Estado: **PLAN TÉCNICO / MAPEO ORM AÑADIDO EN ETAPA 9.6B / SERVICIO
AISLADO DE CREACIÓN VALIDADO EN ETAPA 9.6C.1 / GANADOR Y DESEMPATE POR
PARTICIPACIÓN VALIDADOS EN ETAPA 9.6C.2**.

Última actualización: 2026-07-01.

## 1. Alcance ejecutado

Esta etapa realizó tres acciones:

1. verificó en solo lectura las bases `bingo` y `bingo_ensayo_hibridos`;
2. reaplicó la migración histórica exclusivamente en
   `bingo_ensayo_hibridos` mediante una copia temporal protegida;
3. auditó el código para preparar la refactorización, sin modificarlo.

No se ejecutó ningún cambio sobre la base real `bingo`. No se modificaron
`.env`, modelos, servicios, vistas, formularios, templates, rutas, JavaScript,
CSS, WebSockets, reportes o pruebas.

## 2. Evidencia de bases y migración de ensayo

### 2.1 Estado anterior a la reaplicación

Las consultas se ejecutaron con `default_transaction_read_only=on`:

| Base consultada | `current_database()` | `transaction_read_only` | `carton_partida_bingo` |
|---|---|---|---|
| Real | `bingo` | `on` | No existe |
| Ensayo tras rollback | `bingo_ensayo_hibridos` | `on` | No existía |

### 2.2 Protección usada para reaplicar

Se copió
`DATABASE/02_MIGRACION_CARTONES_HIBRIDOS_PROPUESTA.sql` a
`/tmp/02_MIGRACION_CARTONES_HIBRIDOS_ENSAYO_9_6A.sql`.

La copia temporal cambió únicamente:

- eliminación de `\echo` y `\quit`;
- guardia de base de `bingo` a `bingo_ensayo_hibridos`.

El archivo versionado conservó su hash SHA-256:

```text
9d1a34cb2bd3ec2e92f5b5345ace58461626c837ff4256e2014161c6ae1d405c
```

Inmediatamente antes de la operación mutante se ejecutó:

```sql
SELECT current_database();
```

El resultado fue exactamente `bingo_ensayo_hibridos`. La copia temporal
terminó con `COMMIT`, `UPDATE 12` para la nueva columna `carton.idbingo` e
`INSERT 0 12` para las asignaciones históricas.

### 2.3 Validación posterior

`DATABASE/03_VALIDACION_CARTONES_HIBRIDOS.sql` se ejecutó sobre la copia con
`transaction_read_only=on`.

| Control | Resultado |
|---|---:|
| Cartones maestros | 12 |
| Asignaciones reales | 12 |
| Asignaciones históricas originales | 12 |
| Asignaciones no originales | 0 |
| Asignaciones históricas inferidas | 0 |
| Cartones sin Bingo | 0 |
| Cartones sin partida histórica | 0 |
| Pares cartón/partida duplicados | 0 |
| Inconsistencias de mismo Bingo | 0 |
| Índices migrados incorrectamente | 0 |
| Ganadores inferidos | 0 |
| Recaudación vendida | 41.00 |
| Recaudación histórica total | 51.00 |

Distribución de estados históricos:

- 6 `Cerrado`;
- 1 `En juego`;
- 5 `Pendiente`.

La PK `idcartonpartidabingo` quedó como `IDENTITY BY DEFAULT`, con secuencia
asociada y máximo actual 12.

### 2.4 Reconfirmación de la base real

Después de migrar la copia se volvió a consultar `bingo` en solo lectura:

```text
current_database = bingo
transaction_read_only = on
to_regclass('public.carton_partida_bingo') = NULL
```

La base real continúa en el esquema antiguo.

## 3. Esquema validado en la copia

### `carton`

La expansión agrega `idbingo integer NOT NULL` con FK a `bingo`. Se conservan:

- `idpartida`, nullable, como referencia histórica;
- `indicevictoria`, nullable, como dato histórico;
- dueño, código, matriz, precio, fecha y estado del maestro.

### `carton_partida_bingo`

| Campo | Mapeo físico |
|---|---|
| `idcartonpartidabingo` | `integer GENERATED BY DEFAULT AS IDENTITY`, PK |
| `idcarton` | FK obligatoria al maestro |
| `idpartida` | FK obligatoria a la ronda |
| `idbingo` | Bingo redundante para integridad compuesta |
| `estado_participacion` | `Pendiente`, `En juego`, `Cerrado`, `Ganador` o `Anulado` |
| `indicevictoria` | nullable y, cuando existe, mayor que cero |
| `es_asignacion_original` | distingue las 12 relaciones históricas |
| `origen_asignacion` | `Historica original` o `Aplicacion` |
| `motivoestado` | explicación opcional |
| `fechacreacion` | fecha obligatoria |
| `fechavalidacion` | fecha opcional de validación |

La base garantiza:

- `UNIQUE(idcarton, idpartida)`;
- FK compuesta `(idcarton, idbingo)`;
- FK compuesta `(idpartida, idbingo)`;
- cartón y partida pertenecientes al mismo Bingo.

## 4. Diferencia entre el modelo antiguo y el objetivo

```text
ACTUAL
Carton --idpartida--> Partidabingo --idbingo--> Bingo
  └── indicevictoria global

OBJETIVO
Carton maestro --idbingo-----------------------> Bingo
       |
       +--< CartonPartidaBingo >-- Partidabingo
                 ├── estado por ronda
                 ├── indicevictoria por ronda
                 └── resultado por ronda
```

En el modelo objetivo:

- código, matriz, dueño, precio y compra existen una sola vez;
- el precio se cobra y reporta una sola vez por maestro;
- la participación y el resultado pertenecen a una ronda;
- un cartón puede quedar `Ganador` en varias partidas del mismo Bingo;
- `Carton.idpartida` y `Carton.indicevictoria` nunca son fuente de verdad para
  cartones nuevos.

## 5. Primer cambio requerido en `models.py`

El primer cambio de código debe ser el mapeo exacto del esquema expandido en
`apps/bingos/models.py`. No se deben crear migraciones Django.

### Cambios de `Carton`

1. Agregar `idbingo = models.ForeignKey(...)` con `db_column='idbingo'` y un
   `related_name` explícito, por ejemplo `cartones`.
2. Mantener `idpartida` e `indicevictoria` con sus nombres y columnas actuales
   durante la compatibilidad.
3. Marcar ambos como históricos mediante comentarios y excluirlos de todos los
   formularios de alta nuevos.
4. Los cartones nuevos deben guardar `idpartida=NULL` e
   `indicevictoria=NULL`; solo `idbingo` representa su pertenencia.
5. Mantener `managed = False` y `db_table = 'carton'`.

### Nuevo `CartonPartidaBingo`

Mapeo propuesto:

```python
class CartonPartidaBingo(models.Model):
    idcartonpartidabingo = models.AutoField(primary_key=True)
    idcarton = models.ForeignKey(
        "bingos.Carton",
        models.DO_NOTHING,
        db_column="idcarton",
        related_name="participaciones",
    )
    idpartida = models.ForeignKey(
        "bingos.Partidabingo",
        models.DO_NOTHING,
        db_column="idpartida",
        related_name="participaciones_carton",
    )
    idbingo = models.ForeignKey(
        "bingos.Bingo",
        models.DO_NOTHING,
        db_column="idbingo",
        related_name="participaciones_carton",
    )
    estado_participacion = models.CharField(max_length=20)
    indicevictoria = models.IntegerField(blank=True, null=True)
    es_asignacion_original = models.BooleanField(default=False)
    origen_asignacion = models.CharField(max_length=24)
    motivoestado = models.CharField(max_length=255, blank=True, null=True)
    fechacreacion = models.DateTimeField()
    fechavalidacion = models.DateTimeField(blank=True, null=True)

    class Meta:
        managed = False
        db_table = "carton_partida_bingo"
        unique_together = (("idcarton", "idpartida"),)
```

`AutoField` es obligatorio para que Django trate la PK `IDENTITY` como
autogenerada y recupere el ID insertado. No debe usarse
`assign_next_integer_pk` para esta tabla.

Django no representa aquí las dos FK compuestas como relaciones compuestas.
`idbingo` debe mapearse igualmente y los servicios deben validar igualdad; las
constraints físicas siguen siendo la garantía final.

## 6. Conservación de campos históricos

Durante la fase compatible:

- lecturas históricas pueden mostrar `Carton.idpartida` como trazabilidad, pero
  el flujo funcional debe consultar `CartonPartidaBingo`;
- `Carton.indicevictoria` no se actualiza nunca más;
- las 12 filas migradas tienen `es_asignacion_original=True`;
- los cartones nuevos dejan ambos campos históricos en `NULL`;
- ninguna consulta nueva puede usar `Carton.objects.filter(idpartida=...)` para
  decidir participación;
- la retirada física de ambos campos corresponde a una fase posterior, después
  de demostrar que no quedan lecturas ni escrituras.

Una capa de consultas centralizada debe evitar que cada vista implemente joins
distintos. Como mínimo debe ofrecer:

- participaciones de una partida con maestro y jugador precargados;
- participaciones de un cartón ordenadas por ronda;
- cartones maestros de un Bingo sin duplicación;
- participación exacta para la pareja cartón/partida.

## 7. Generación de cartones nuevos

La operación debe ser atómica y pertenecer a la capa de servicios:

1. recibir un Bingo, jugador, precio y fecha, no una única partida como dueño;
2. bloquear la fila de Bingo y obtener con bloqueo estable sus partidas;
3. validar que la venta todavía está permitida;
4. generar una sola matriz y un solo código, preferiblemente con prefijo de
   Bingo en lugar de partida;
5. crear un solo `Carton` con `idbingo`, precio único e históricos en `NULL`;
6. crear exactamente una `CartonPartidaBingo` por cada partida existente, con
   `origen_asignacion='Aplicacion'`;
7. no copiar precio ni matriz a las participaciones;
8. revertir todo si falla una sola participación.

Si existen `N` partidas, deben quedar un maestro y `N` participaciones. La
recaudación aumenta una sola vez.

Debe definirse además el caso inverso: si se permite agregar una partida a un
Bingo después de vender cartones, `partida_nueva` debe crear, en la misma
transacción, una participación para cada maestro existente. Ambos servicios
deben bloquear el Bingo en el mismo orden para evitar carreras.

La alternativa más segura es cerrar la venta cuando alguna ronda ya empezó. De
lo contrario, crear un cartón después de rondas finalizadas produciría filas
para rondas pasadas y volvería a introducir una inferencia histórica.

## 8. Inventario exacto de código que requiere cambio

### 8.1 Modelos, formularios y administración

| Archivo | Símbolos | Cambio requerido |
|---|---|---|
| `apps/bingos/models.py` | `Carton`; nuevo `CartonPartidaBingo` | Mapear `idbingo`, la tabla intermedia y la PK `AutoField`; conservar campos históricos |
| `apps/bingos/forms.py` | `CartonForm`, `GenerarAsignarCartonForm`, `CartonPartidaForm` | Quitar edición funcional de `idpartida`/`indicevictoria`; separar formulario maestro y formulario de participación; contextualizar la compra por Bingo |
| `apps/bingos/admin.py` | registro de `Carton`; eventual registro de `CartonPartidaBingo` | Impedir altas directas que evadan el servicio atómico; preferir administración de solo lectura para la intermedia |

`BingoForm` no requiere cambio. `PartidaBingoForm` solo necesita cambio si la
creación de una ronda dispara participaciones para cartones ya vendidos; esa
operación debe vivir en servicio, no en `form.save()`.

### 8.2 Servicios

`apps/bingos/services.py` requiere cambios en estos símbolos:

- generación: `puede_asignar_cartones`, `validar_asignacion_cartones`,
  `generar_codigo_carton`, `crear_y_asignar_carton`;
- pertenencia y evaluación: `_carton_pertenece_a_partida`,
  `_carton_vendido_y_asignado`, `evaluar_carton_en_partida`,
  `buscar_cartones_ganadores`;
- presentación por ronda: `preparar_datos_carton_jugador`,
  `preparar_cartones_para_validacion`;
- desempate: `_candidato_desempate_desde_carton`,
  `_serializar_cartones_ganadores_desempate`,
  `_normalizar_carton_candidato`, `_agregar_carton_candidato`,
  `normalizar_candidatos_desempate`, `serializar_candidatos_desempate`,
  `_obtener_candidatos_partida_desempate`, `preparar_datos_desempate`,
  `sortear_balota_desempate`, `confirmar_y_finalizar_desempate`;
- resultado: `validar_carton_ganador`.

Se deben agregar servicios explícitos para:

- crear maestro y participaciones;
- crear participaciones cuando se agrega una ronda posterior;
- consultar/ bloquear participaciones de una partida;
- cambiar estados de participación al iniciar, cancelar o cerrar una ronda;
- marcar la participación exacta ganadora sin escribir
  `Carton.indicevictoria`.

`extraer_siguiente_bola` puede conservar su persistencia sobre
`Partidabingo`. El progreso visual se deriva de la matriz maestra y las bolas
de esa partida; no debe escribir el precio ni duplicar el cartón. Debe someterse
a regresión porque sus eventos actualizan la ronda seleccionada en las vistas
de cartón.

### 8.3 Vistas

`apps/bingos/views.py` requiere cambio en:

- consulta pública/privada: `acceder_carton_publico`, `carton_publico`,
  `mis_cartones`, `mi_carton_detalle`, `_resumen_carton_privado`;
- Bingo: `bingo_detalle`, `bingo_resumen_excel`;
- ronda: `partida_nueva`, `partida_detalle`, `partida_reporte_pdf`,
  `partida_cartones_excel`;
- alta/edición: `partida_carton_nuevo`, `partida_carton_editar`,
  `carton_nuevo`, `carton_editar`;
- operación: `consola_operador`, `validar_carton`;
- desempate: `desempate_operador`, `sortear_desempate`,
  `confirmar_desempate`.

`sacar_bola` conserva el objeto partida, pero debe seguir publicando al grupo
de la ronda exacta. `_procesar_accion_consola` debe coordinar además los estados
de participación cuando una ronda inicia, se cancela o finaliza.

`apps/jugadores/views.py`, método `_detalle_context`, debe precargar `idbingo`
y participaciones en vez de `idpartida__idbingo`.

### 8.4 Reportes

`apps/bingos/reportes.py` requiere cambio en:

- `construir_datos_reporte_partida`;
- `generar_pdf_reporte_partida`;
- `generar_excel_cartones_partida`;
- `generar_excel_resumen_bingo`;
- `_carton_ganador`.

Los reportes de partida deben recibir participaciones y leer el maestro para
código, matriz y jugador. El resumen de Bingo debe:

- contar maestros distintos;
- sumar `Carton.preciopagado` una sola vez por maestro;
- contar participaciones por ronda por separado;
- obtener el cartón ganador desde una participación marcada `Ganador`, no por
  coincidencia heurística con `idjugadorganador`.

### 8.5 URLs y templates

`apps/bingos/urls.py` debe introducir una alta por Bingo, por ejemplo
`bingos/<idbingo>/cartones/nuevo/`. La ruta antigua por partida puede quedar
temporalmente como redirección compatible. La pareja `(idpartida, idcarton)`
sigue identificando una participación por su UNIQUE.

Templates que requieren adaptación funcional:

- `templates/bingos/carton_formulario.html`;
- `templates/bingos/cartones_lista.html`;
- `templates/bingos/partida_carton_generar.html`;
- `templates/bingos/partida_carton_formulario.html`;
- `templates/bingos/partida_detalle.html`;
- `templates/bingos/consola_operador.html`;
- `templates/bingos/desempate_operador.html`;
- `templates/bingos/detalle.html`;
- `templates/bingos/mis_cartones.html`;
- `templates/bingos/mi_carton_detalle.html`;
- `templates/bingos/carton_publico.html`;
- `templates/jugadores/detalle.html`.

No se identificó cambio obligatorio inicial en `carton_acceso_publico.html`,
`sala_juego_publica.html`, `tablero_publico.html`, navbar o sidebar; sus enlaces
pueden conservarse.

### 8.6 WebSockets y JavaScript

Se auditaron:

- `apps/bingos/consumers.py`: `PartidaPublicaConsumer`;
- `apps/bingos/realtime.py`: `nombre_grupo_partida`,
  `construir_payload_publico_partida`, `programar_publicacion_partida`;
- `apps/bingos/routing.py`: `websocket_urlpatterns`;
- `static/js/realtime_bingo.js`.

El contrato ya está correctamente particionado por `idpartidabingo`. No se
requiere cambiar el canal si la vista de cartón selecciona una participación y
renderiza su `idpartida` en `data-partida-id`. La primera implementación debe
usar selector de ronda con recarga de servidor; así el JavaScript puede quedar
sin cambios. Si se decide cambiar de ronda sin recargar, entonces
`realtime_bingo.js` deberá cerrar el socket anterior y suscribirse al nuevo.

El payload público no debe incluir matriz, precio, propietario, otras
participaciones ni candidatos privados de desempate.

### 8.7 Pruebas afectadas

En `apps/bingos/tests.py` requieren actualización directa estas clases:

- `AsignacionCartonTests`, `GenerarAsignarCartonFormTests` y
  `FlujoAsignacionCartonTests`;
- `ValidacionGanadorTests` y `RutaValidacionCartonTests`;
- `FormatoCandidatosDesempateTests`, `ServicioDesempateTests` y
  `RutasDesempateTests`;
- `ServiciosPublicosBingoTests`, `VistasPublicasBingoTests` y
  `PlantillasPublicasBingoTests`;
- `ConsolaOperadorTests`;
- `WebSocketPublicoBingoTests`, `PayloadPublicoTiempoRealTests`,
  `IntegracionPublicacionTiempoRealTests` y
  `PlantillasTiempoRealBingoTests`;
- `ReportesAdministrativosTests`.

También deben cambiar los helpers `carton_publico_prueba`, las fábricas
`_carton` de las clases afectadas y los mocks que hoy interceptan
`Carton.objects.filter(idpartida=...)`.

`GeneracionMatrizCartonTests`, `ReglaGanadorCartonTests` y
`ExtraccionBolasTests` deben mantenerse como regresión; sus fixtures deberán
incorporar Bingo/participación cuando llamen servicios integrados.

Fuera del módulo, `apps/seguridad/tests.py` requiere ajustar
`PermisosAdministrativosTests` en sus escenarios públicos y
`MisCartonesPrivadoTests` completo. Deben añadirse pruebas para
`apps/jugadores/views.py::_detalle_context` y
`templates/jugadores/detalle.html`.

## 9. Adaptación funcional por flujo

### Consola del operador

- consultar participaciones de la partida y precargar maestro/jugador;
- mostrar estado e índice de la participación, no el índice histórico;
- validar mediante la pareja cartón/partida;
- editar por separado datos maestros y datos de ronda;
- mantener generación por Bingo, aunque se llegue desde una ronda.

### Extracción de bolas

- conservar bolas y última bola en `Partidabingo`;
- calcular progreso usando la matriz maestra y bolas de la ronda seleccionada;
- no escribir progreso global en `Carton`;
- publicar solo el grupo WebSocket de esa partida.

### Validación de ganador

- comprobar existencia de `CartonPartidaBingo` para la pareja;
- bloquear partida, participaciones candidatas y maestros necesarios;
- evaluar matrices maestras contra bolas de esa partida;
- marcar la participación exacta como `Ganador` y fecha de validación;
- definir y guardar `indicevictoria` positivo por ronda;
- permitir que el mismo `idcarton` gane otra partida del mismo Bingo.

### Desempate

- persistir en candidatos `idcartonpartidabingo`, además de cartón y jugador;
- no perder varios cartones del mismo jugador;
- al confirmar, actualizar la participación exacta ganadora;
- conservar `Partidabingo.idjugadorganador` como resumen compatible del jugador.

### Mis cartones

- listar una fila por maestro, agrupada por Bingo;
- no repetir precio o matriz por ronda;
- detalle con selector/lista de participaciones;
- progreso y estado calculados para la ronda seleccionada;
- mantener control de propiedad por `Carton.idjugador`.

### Vista pública

- resolver el código global a un maestro;
- mostrar Bingo y una lista controlada de rondas;
- exigir que la ronda seleccionada tenga una participación del cartón;
- elegir de forma determinista la ronda inicial: activa, luego pendiente y por
  último la cerrada más reciente;
- no inferir participación desde `Carton.idpartida`.

### PDF y Excel

- reporte de partida: filas de participación con datos del maestro;
- índice/estado desde la participación;
- resumen de Bingo: maestros y recaudación únicos, participaciones por ronda;
- ganador desde la participación, sin inferirlo por propietario;
- conservar exclusión de datos privados.

## 10. Orden recomendado de refactorización

1. Mapear `Carton.idbingo` y `CartonPartidaBingo` en `models.py`.
2. Crear constantes de estado/origen y consultas centralizadas.
3. Refactorizar generación atómica y alta posterior de partidas.
4. Refactorizar evaluación, ganador y desempate sobre participaciones.
5. Añadir primero pruebas unitarias de servicios y de invariantes.
6. Adaptar formularios y vistas administrativas.
7. Adaptar Mis cartones y consulta pública.
8. Adaptar reportes para evitar duplicar recaudación.
9. Adaptar templates y rutas, conservando redirecciones compatibles.
10. Verificar el contrato WebSocket por ronda y modificar JavaScript solo si el
    selector no recarga la página.
11. Ejecutar pruebas de integración contra una copia migrada aislada.

No debe iniciarse una fase dependiente mientras la anterior aún use
`Carton.idpartida` como fuente funcional.

## 11. Casos de prueba obligatorios antes de tocar `bingo`

1. El mapeo unmanaged lee los 12 maestros y 12 participaciones históricas.
2. `AutoField` inserta una participación y recupera su IDENTITY sin calcular
   `MAX(id)+1`.
3. Un Bingo con `N` partidas crea un maestro y exactamente `N` participaciones.
4. Un fallo en una participación revierte maestro, precio y todas las filas.
5. La matriz y el código existen una sola vez; el precio se suma una sola vez.
6. Una partida agregada después crea una fila para cada maestro existente, si
   esa operación queda permitida.
7. Dos altas concurrentes no duplican código, PK ni pareja cartón/partida.
8. Las FK compuestas rechazan mezclar Bingos.
9. `UNIQUE(idcarton,idpartida)` rechaza duplicados.
10. Los cartones nuevos dejan históricos `idpartida` e `indicevictoria` en
    `NULL`.
11. Los 12 cartones antiguos siguen visibles solo en su participación original.
12. Un cartón puede ganar dos rondas distintas del mismo Bingo.
13. Validar una ronda no cambia resultado ni progreso de otra.
14. Varios ganadores generan desempate con la participación exacta.
15. Mis cartones lista maestros una sola vez y protege propiedad.
16. La consulta pública no permite seleccionar una ronda de otro Bingo/cartón.
17. PDF y Excel de Bingo mantienen recaudación `41.00`/`51.00` en el histórico
    y no multiplican precios por participación.
18. La consola, extracción y WebSocket afectan solo la ronda seleccionada.
19. Los payloads públicos no filtran datos privados.
20. `python manage.py check` sigue limpio y no aparece ninguna migración Django
    para modelos `managed=False`.

Las pruebas con base deben ejecutarse sobre una restauración aislada o una base
de ensayo recreable, nunca sobre `bingo`.

## 12. Riesgos restantes

- Semántica exacta de `indicevictoria`: debe fijarse si representa número de
  bolas extraídas, orden de validación u otra métrica positiva.
- Ventana de venta: debe impedir crear filas para rondas ya jugadas.
- Partidas creadas después de vender cartones: requiere backfill transaccional
  o prohibición explícita.
- `Partidabingo.idjugadorganador` guarda jugador, no participación; el detalle
  exacto debe permanecer en la intermedia.
- El admin genérico y formularios directos pueden evadir invariantes.
- La FK compuesta es garantizada por PostgreSQL, no completamente expresada por
  el ORM.
- Reportes pueden duplicar recaudación si agregan por participación.
- Un rollback simple deja de ser seguro después de crear cartones híbridos
  nuevos con históricos en `NULL`.
- La base real todavía no posee las columnas/tablas que exigirán los modelos
  nuevos; no se puede desplegar ese código antes de la expansión.

## 13. Estrategia de despliegue final

La migración física y el despliegue del código híbrido requieren una ventana
sin escrituras de cartones. En cuanto `carton.idbingo` pasa a `NOT NULL`, el
código anterior deja de poder crear cartones: su `INSERT` no envía `idbingo` y
PostgreSQL lo rechaza. Que las columnas nuevas no rompan ciertas lecturas del
código antiguo no convierte ese código en compatible para escritura.

1. Completar código y pruebas sobre `bingo_ensayo_hibridos` migrada.
2. Preparar artefacto de aplicación compatible con el esquema expandido.
3. Tomar respaldo y restaurarlo con éxito.
4. Abrir ventana y detener, como mínimo, todas las escrituras de cartones y la
   creación de partidas que pueda generar participaciones.
5. Confirmar `SELECT current_database()` y ejecutar la migración histórica en
   `bingo` solo con autorización independiente.
6. Validar 12/12/0 y controles de integridad.
7. Desplegar inmediatamente el código híbrido, sin reactivar el flujo anterior
   de creación de cartones durante el intervalo.
8. Ejecutar checks, smoke tests presenciales/virtuales, generación de un cartón
   controlado, consola, ganador, Mis cartones y reportes.
9. Reabrir escrituras únicamente si todos los controles pasan.
10. Observar y reconciliar maestros, participaciones y recaudación.
11. Retirar `Carton.idpartida` y `Carton.indicevictoria` solo en una etapa
    posterior, cuando búsquedas estáticas, métricas y pruebas demuestren que ya
    no se usan.

Si falla algo antes de escrituras híbridas, se debe mantener la ventana cerrada,
restaurar el código anterior y aplicar el rollback ensayado antes de permitirle
crear cartones otra vez. Después de escrituras nuevas se requiere una reversión
específica que preserve maestros y participaciones; no se debe usar el rollback
simple.

## 14. Validaciones locales de la Etapa 9.6A

| Control | Resultado |
|---|---|
| `python manage.py check` inicial | Sin problemas |
| Archivo versionado de migración | Sin cambios |
| Código Django y assets | Sin cambios |
| Commit / push | No realizados |
| `python manage.py check` final | `System check identified no issues (0 silenced).` |
| `git diff --check` final | Sin errores ni salida |
| `git status --short` final | Solo este documento nuevo, sin seguimiento |

## 15. Actualización de mapeo en la Etapa 9.6B

Sin generar migraciones Django, `apps/bingos/models.py` incorpora:

- `Carton.idbingo` como FK obligatoria hacia `Bingo`;
- comentarios de compatibilidad sobre `Carton.idpartida` y
  `Carton.indicevictoria`;
- `CartonPartidaBingo` con los once campos físicos validados;
- `AutoField` para la PK `IDENTITY BY DEFAULT`;
- `managed = False`, tabla física y unicidad cartón/partida;
- constantes mínimas de estado y origen.

La comprobación de este mapeo debe ejecutarse únicamente con variables
temporales apuntando a `bingo_ensayo_hibridos` y con la sesión PostgreSQL en
solo lectura. No se debe iniciar el servidor ni probar formularios mientras la
configuración efectiva apunte a la base real sin expandir.

| Control 9.6B | Resultado |
|---|---|
| Base efectiva / sesión | `bingo_ensayo_hibridos` / solo lectura `on` |
| `python manage.py check` sobre ensayo | Sin problemas |
| Conteos ORM | 12 cartones / 12 participaciones / 12 originales / 0 no originales |
| Cartones con `idbingo IS NULL` | 0 |
| PK ORM | `AutoField`; primer valor validado: 1 |
| Relaciones del primer registro | Carton, Partidabingo y Bingo válidos; mismo Bingo |
| `git diff --check` | Sin errores ni salida |

## 16. Servicio aislado de creación en la Etapa 9.6C.1

`apps/bingos/services.py` incorpora, sin reemplazar el flujo heredado
`crear_y_asignar_carton(...)`, los siguientes símbolos:

- `generar_codigo_carton_bingo(...)`, que emite códigos globales con formato
  `B<idbingo>-C-<token>`;
- `validar_venta_carton_para_bingo(...)`, que exige al menos una partida y
  permite la venta únicamente si todas están en `Programada` o `En espera`;
- `crear_carton_maestro_para_bingo(...)`, que recibe Bingo, jugador, precio y
  fecha opcional, y ejecuta toda la creación dentro de `transaction.atomic()`.

El servicio bloquea con `select_for_update()` el Bingo y sus partidas. Genera
un solo maestro con dueño, precio, matriz, código y estado `Vendido`; para los
cartones nuevos, `Carton.idpartida` y `Carton.indicevictoria` permanecen en
`NULL`. La PK manual mediante `assign_next_integer_pk` se aplica únicamente al
maestro heredado. Cada partida recibe una participación `Pendiente`, con
`indicevictoria=NULL`, `es_asignacion_original=False`,
`origen_asignacion='Aplicacion'` y `fechavalidacion=NULL`; su PK la asigna
PostgreSQL mediante `IDENTITY`/`AutoField`. De este modo, el maestro se cobra
una sola vez y tanto la matriz como el código se generan una sola vez.

La política de venta rechaza `En curso`, `Pausada`, `Desempate`, `Finalizada`,
`Cancelada` y cualquier estado distinto de los dos permitidos. Si falla la
creación de una participación, el error se propaga para revertir maestro,
participaciones, precio y cualquier cambio parcial.

### 16.1 Pruebas unitarias aisladas

Se ejecutó únicamente `CartonMaestroBingoTests` con
`DB_NAME=bingo_ensayo_hibridos` y PostgreSQL en solo lectura. Las seis pruebas
pasaron y cubren:

- maestro único y una participación por partida;
- prefijo de Bingo en el código;
- rechazo de un Bingo sin partidas;
- rechazo de partidas no aptas;
- aceptación conjunta de `Programada` y `En espera`;
- propagación del error de una participación para rollback atómico.

Django informó `Skipping setup of unused database(s): default.`, por lo que
estas pruebas unitarias no prepararon una base mutante.

### 16.2 Prueba temporal sobre ensayo

Antes de habilitar escrituras temporales se ejecutó
`SELECT current_database();` y el resultado fue exactamente
`bingo_ensayo_hibridos`. El smoke reutilizó un jugador existente y creó dentro
de una transacción externa un Bingo, dos partidas vendibles, un maestro y dos
participaciones. Durante la transacción se comprobó:

| Control durante el smoke | Resultado |
|---|---|
| Conteos | 13 cartones / 14 participaciones / 12 originales / 2 no originales |
| Maestro temporal | Uno; `idbingo` definido, históricos en `NULL`, precio único |
| Código y matriz | Un código con prefijo de Bingo y una matriz Bingo 75 válida |
| Participaciones | Dos, `Pendiente`, origen `Aplicacion`, no originales |
| PK de participaciones | Asignadas automáticamente: 13 y 14 |
| Históricos | Las instantáneas de los 12 cartones y 12 participaciones no cambiaron |

El script ejecutó explícitamente `transaction.set_rollback(True)`. Tras salir
de la transacción confirmó el rollback total: no quedaron el Bingo, partidas,
maestro ni participaciones temporales, ni nombres que contengan
`SMOKE 9.6C.1`. Los conteos volvieron exactamente a 12 cartones, 12
participaciones, 12 originales y 0 no originales.

La secuencia
`public.carton_partida_bingo_idcartonpartidabingo_seq` pasó temporalmente de
`(last_value=12, is_called=true)` a `(14, true)` y fue restaurada exactamente a
`(12, true)`. Una comprobación independiente posterior, nuevamente con
`transaction_read_only=on`, reconfirmó conteos, ausencia del marcador y estado
de la secuencia.

La búsqueda estática final solo encuentra el servicio nuevo en
`apps/bingos/services.py` y sus pruebas en `apps/bingos/tests.py`: ninguna
vista ni formulario lo utiliza todavía. En esta etapa tampoco se modificaron
rutas, templates, reportes, WebSockets, JavaScript, CSS, administración ni
`.env`, y no se escribió en la base real `bingo`.

## 17. Ganador y desempate por participación en la Etapa 9.6C.2

`apps/bingos/services.py` incorpora un flujo híbrido paralelo, sin modificar ni
reemplazar `_carton_pertenece_a_partida(...)`,
`evaluar_carton_en_partida(...)`, `buscar_cartones_ganadores(...)`,
`validar_carton_ganador(...)` ni los servicios de desempate heredados. Los
nuevos servicios principales son:

- `obtener_participacion_carton_en_partida(...)`, que resuelve la pareja exacta
  `(idcarton, idpartida)` y comprueba el mismo Bingo;
- `evaluar_participacion_en_partida(...)` y
  `buscar_participaciones_ganadoras(...)`, que usan la matriz del maestro y
  exclusivamente las bolas de la ronda recibida;
- `validar_participacion_ganadora(...)`, que bloquea partida, participaciones y
  maestros, y guarda `Ganador`, índice positivo y fecha únicamente en
  `CartonPartidaBingo`;
- `construir_candidato_desempate_participacion(...)`,
  `normalizar_candidatos_desempate_participaciones(...)` y
  `serializar_candidatos_desempate_participaciones(...)`, que conservan
  `idcartonpartidabingo`, cartón, partida, Bingo, jugador y código sin agrupar
  solo por jugador;
- `sortear_balota_desempate_participacion(...)` y
  `confirmar_y_finalizar_desempate_participaciones(...)`, que operan y
  actualizan candidatas de una única ronda.

El resultado funcional vive en cada participación. El servicio no escribe
`Carton.idpartida` ni `Carton.indicevictoria`; ambos permanecen en `NULL` para
maestros nuevos. `Partidabingo.idjugadorganador` se conserva solo como resumen
compatible derivado de la participación ganadora. Dos cartones distintos del
mismo jugador permanecen como candidatas independientes, y la confirmación de
desempate marca la participación exacta ganadora y cierra únicamente las demás
candidatas de esa ronda.

Esta separación permite que un mismo maestro gane varias rondas del mismo
Bingo. Cada ronda mantiene su propio estado, índice y fecha, sin leer ni
sobrescribir el resultado de otra participación.

### 17.1 Pruebas unitarias aisladas

Se ejecutó únicamente `ParticipacionGanadoraHibridaTests` con
`DB_NAME=bingo_ensayo_hibridos` y PostgreSQL en solo lectura. Las 11 pruebas
pasaron y cubren resolución exacta, ausencia de participación, coherencia de
Bingo, bolas de la ronda correcta, campos históricos intactos, dos victorias
independientes del mismo maestro, índice positivo, múltiples ganadoras sin
elección arbitraria, dos candidatas del mismo jugador, rechazo de otra ronda y
confirmación de desempate limitada a sus candidatas. Django informó
`Skipping setup of unused database(s): default.`.

### 17.2 Smoke temporal con rollback

Antes de cada intento se ejecutó `SELECT current_database();` y el resultado
fue exactamente `bingo_ensayo_hibridos`. El primer intento encontró una
aserción demasiado estricta del script al comparar directamente un datetime
Django con una columna PostgreSQL `timestamp without time zone`; aun así, el
bloque de recuperación confirmó inmediatamente `12 / 12 / 12 / 0`, cero
marcadores y secuencia `(12, true)`. Se corrigió únicamente esa aserción
temporal, sin cambiar el servicio.

El segundo intento creó dentro de una transacción externa un Bingo con marcador
`SMOKE 9.6C.2`, dos rondas, un maestro y sus dos participaciones. La matriz del
maestro completó ambas rondas. Primero se validó la participación de la ronda 1
y se comprobó que la ronda 2 continuaba `Pendiente`; después se validó la ronda
2. El resultado durante la transacción fue:

| Control 9.6C.2 | Resultado |
|---|---|
| Maestro | El mismo `idcarton=13` en ambas rondas; históricos en `NULL` |
| Participaciones | IDs 13 y 14, ambas `Ganador` |
| Índices por ronda | 24 y 25 |
| Fechas | No nulas, independientes y guardadas por participación |
| Conteos temporales | 13 cartones / 14 participaciones / 12 originales / 2 no originales |
| Históricos | Instantáneas de 12 cartones y 12 participaciones sin cambios |

El script ejecutó `transaction.set_rollback(True)`. Tras el rollback no quedaron
Bingo, partidas, maestro, participaciones ni nombres con `SMOKE 9.6C.2`. Los
conteos volvieron a 12 cartones, 12 participaciones, 12 originales y 0 no
originales. La secuencia de participaciones pasó temporalmente de `(12, true)`
a `(14, true)` y fue restaurada exactamente a `(12, true)`. Una verificación
independiente posterior confirmó lo anterior con `transaction_read_only=on`.

La búsqueda estática mantiene los servicios nuevos limitados a
`apps/bingos/services.py` y sus pruebas. Ninguna vista ni formulario usa aún el
flujo híbrido de ganador/desempate. No se modificaron rutas, templates,
reportes, WebSockets, JavaScript, CSS, administración ni `.env`, y la base real
`bingo` no fue consultada para escrituras ni modificada.
